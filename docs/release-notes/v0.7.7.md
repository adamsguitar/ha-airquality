## Managed dashboard

- Removed the extra **Measurements** heading above each room’s tile grid so the section shows only the room title, status/summary text, and measurement tiles.

Call **airquality.sync_dashboard** or reload the integration after upgrading.
