## Air Quality UI (add-on)

- **Threshold profiles:** fixed a crash when loading the profiles page (`UndefinedError` iterating reference breakpoints). Jinja was resolving `.values` as the dict method instead of the reference map key.

Reload the integration or refresh the UI after upgrading if you rely on YAML-driven behavior.
