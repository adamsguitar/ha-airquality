## Managed dashboard

- **Household** summary and home overall / problem indicators use the sections view **header** and **view badges** instead of a separate first section.
- **Measurement** tiles use a **2-column** grid so each tile is wider.

Call **airquality.sync_dashboard** or reload the integration after upgrading.
