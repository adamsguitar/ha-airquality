{
  "config": {
    "step": {
      "user": {
        "title": "Air Quality",
        "description": "This integration reads your air quality configuration from `/config/airquality.yaml`.\n\nIf the file does not exist, an example configuration will be created automatically.\n\nClick **Submit** to complete setup.",
        "data": {}
      }
    },
    "error": {
      "yaml_invalid": "The configuration file contains errors. Check your Home Assistant logs for details.",
      "yaml_not_found": "Could not create the example configuration file. Check filesystem permissions.",
      "area_not_found": "One or more `area` values in your configuration do not match a valid Home Assistant area. Check the area_id values."
    },
    "abort": {
      "already_configured": "Air Quality is already configured."
    }
  },
  "services": {
    "reload": {
      "name": "Reload",
      "description": "Reload the air quality configuration from /config/airquality.yaml without restarting Home Assistant."
    },
    "recompute": {
      "name": "Recompute",
      "description": "Force immediate recomputation of all slot values."
    },
    "set_threshold_profile": {
      "name": "Set threshold profile",
      "description": "Temporarily override a space's threshold profile at runtime. Not persisted to YAML.",
      "fields": {
        "area": {
          "name": "Area",
          "description": "The HA area_id of the space to override."
        },
        "profile": {
          "name": "Profile",
          "description": "Name of the threshold profile to apply."
        }
      }
    }
  }
}
